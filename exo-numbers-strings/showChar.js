let msg = "C'était à Mégara, faubourg de Carthage, dans les jardins d'Hamilcar."

console.log(`Tiré du livre : \nSalammbô de Gustave Flaubert (1862) `)

// Afficher chaque caractères d'une string et son index
for (let i = 0; i < msg.length; ++i) {
  console.log(`le charactere ${msg[i]} est a l'index: ${i}`)
}